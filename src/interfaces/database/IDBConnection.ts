export type IDBConnection = FirebaseFirestore.CollectionReference;

// export abstract class IDBConnection {
//   constructor(collectionName: string) {}

//   abstract execute(field: string, operator: WhereFilterOp, value: string): any;
// }
